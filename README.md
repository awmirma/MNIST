# MNIST
starter project in my new career ML &amp; DL


model.py contains loading MNIST  dataset , compiling and saving it in ./handwritten.model

you can use your own hand written digits and save it in ./digits with the format ./digits/digit{digit_number}.png

also you need to have tensorflow , cv2 , numpy and matplotlib libraries
